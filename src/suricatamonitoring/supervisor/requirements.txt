# RiskNoX Supervisor - Python Requirements
# Install with: pip install -r requirements.txt

# Core dependencies for supervisor
pyyaml>=6.0.1
psutil>=5.9.5

# Flask backend dependencies (already in .venv)
flask>=2.3.0
flask-cors>=4.0.0
requests>=2.31.0

# Windows-specific
pywin32>=306

# Scheduling
schedule>=1.2.0

# Build dependencies (optional, for building executable)
pyinstaller>=6.0.0
