<!--
  Local rules for enhanced security detection
  File: /var/monitoring-agent/etc/local_rules.xml
-->

<group name="local,syslog,">
  
  <!-- Enhanced Nmap Detection -->
  <rule id="100001" level="8">
    <if_group>command</if_group>
    <match>nmap|masscan|zenmap</match>
    <description>Network scanning tool detected in process list</description>
    <group>recon,scanning,</group>
  </rule>
  
  <!-- SSH Brute Force Enhanced -->
  <rule id="100002" level="10">
    <if_group>authentication_failed</if_group>
    <match>ssh|sshd</match>
    <description>SSH authentication failure detected</description>
    <group>authentication_failed,ssh,</group>
  </rule>
  
  <!-- Netcat/Reverse Shell Detection -->
  <rule id="100003" level="12">
    <if_group>command</if_group>
    <match>nc -l|netcat -l|/bin/sh|/bin/bash</match>
    <regex>\d+\.\d+\.\d+\.\d+</regex>
    <description>Possible reverse shell or netcat listener detected</description>
    <group>intrusion,reverse_shell,</group>
  </rule>
  
  <!-- Multiple Connection Attempts -->
  <rule id="100004" level="6">
    <if_group>command</if_group>
    <match>LISTEN</match>
    <regex>:\d{4,5}\s</regex>
    <description>New network service listening on high port</description>
    <group>network,listening_ports,</group>
  </rule>
  
  <!-- Enhanced Sudo Detection -->
  <rule id="100005" level="4">
    <if_group>sudo</if_group>
    <match>COMMAND=</match>
    <description>Sudo command execution detected</description>
    <group>privilege_escalation,sudo,</group>
  </rule>
  
  <!-- File Access in Sensitive Directories -->
  <rule id="100006" level="7">
    <if_group>command</if_group>
    <match>/etc/passwd|/etc/shadow|/etc/sudoers</match>
    <description>Access to sensitive system files detected</description>
    <group>privilege_escalation,sensitive_files,</group>
  </rule>
  
  <!-- Log Injection Detection -->
  <rule id="100007" level="10">
    <match>SECURITY_TEST|WAZUH_TEST</match>
    <description>Security test injection detected in logs</description>
    <group>log_injection,testing,</group>
  </rule>

</group>

<!-- Frequency-based rules for enhanced detection -->
<group name="local,frequency,">
  
  <!-- Multiple failed connections -->
  <rule id="100101" level="10" frequency="5" timeframe="120">
    <if_matched_sid>100002</if_matched_sid>
    <same_source_ip />
    <description>Multiple SSH authentication failures from same IP</description>
    <group>brute_force,ssh,</group>
  </rule>
  
  <!-- Multiple sudo commands -->
  <rule id="100102" level="8" frequency="3" timeframe="300">
    <if_matched_sid>100005</if_matched_sid>
    <same_user />
    <description>Multiple sudo commands by same user in short time</description>
    <group>privilege_escalation,suspicious_activity,</group>
  </rule>

</group>

<!-- ============================================================ -->
<!--  ClamAV Active Response Rules                                -->
<!--  Triggers manual ClamAV scans on Windows agents             -->
<!-- ============================================================ -->
<group name="clamav,">

  <!-- Base rule: Detect the trigger log -->
  <rule id="100499" level="3">
    <decoded_as>clamav_trigger</decoded_as>
    <description>ClamAV manual scan trigger received</description>
    <group>clamav,trigger,</group>
  </rule>

  <!-- Active Response trigger rule - level 10 required to fire AR -->
  <rule id="100500" level="10">
    <if_sid>100499</if_sid>
    <description>Manual ClamAV scan triggered from manager</description>
    <group>clamav,manual_scan,</group>
  </rule>

  <!-- Quick scan rule -->
  <rule id="100501" level="5">
    <if_sid>100500</if_sid>
    <field name="scan_type">quickscan</field>
    <description>Quick ClamAV scan triggered on agent</description>
    <group>clamav,quickscan,</group>
  </rule>

  <!-- Full scan rule -->
  <rule id="100502" level="7">
    <if_sid>100500</if_sid>
    <field name="scan_type">fullscan</field>
    <description>Full system ClamAV scan triggered on agent</description>
    <group>clamav,fullscan,</group>
  </rule>

</group>

<!-- ============================================================ -->
<!--  GROUP 1: Windows System State Events                        -->
<!--  Parent: 18104 (Windows audit success/event catch-all)       -->
<!--  Uses <id> element to match WinEvtLog decoded EventID field  -->
<!--  IDs 100200-100209 (avoid collision with frequency rules)    -->
<!-- ============================================================ -->
<group name="local,windows,system_state,">

  <!-- EventID 6005: Event Log service started = System boot/startup -->
  <!-- Overrides built-in rule 83202 (level 5) by being its child -->
  <rule id="100200" level="8">
    <if_sid>83202</if_sid>
    <id>6005</id>
    <description>Windows: System startup - Event Log service started (boot detected)</description>
    <group>windows,system_state,pci_dss_10.6.1,gpg13_10.1,</group>
  </rule>

  <!-- EventID 6006: Event Log service stopped = Clean shutdown -->
  <rule id="100201" level="8">
    <if_sid>18101</if_sid>
    <id>6006</id>
    <description>Windows: Clean shutdown - Event Log service stopped</description>
    <group>windows,system_state,pci_dss_10.6.1,gpg13_10.1,</group>
  </rule>

  <!-- EventID 6008: Unexpected shutdown = Crash or power loss -->
  <rule id="100202" level="12">
    <if_sid>18101</if_sid>
    <id>6008</id>
    <description>Windows: CRITICAL - Unexpected shutdown detected (crash or power loss)</description>
    <group>windows,system_state,pci_dss_10.6.1,gpg13_10.1,</group>
  </rule>

  <!-- EventID 1074: Planned restart or shutdown by user/process -->
  <rule id="100203" level="8">
    <if_sid>18101</if_sid>
    <id>1074</id>
    <description>Windows: Planned restart or shutdown initiated by user</description>
    <group>windows,system_state,pci_dss_10.6.1,gpg13_10.1,</group>
  </rule>

  <!-- EventID 4608: Security auditing started = OS fully booted -->
  <rule id="100204" level="5">
    <if_sid>18104</if_sid>
    <id>4608</id>
    <description>Windows: Security auditing started - system fully booted</description>
    <group>windows,system_state,</group>
  </rule>

</group>

<!-- ============================================================ -->
<!--  GROUP 2: Sensitive Privilege Use                            -->
<!--  IDs 100210-100219                                           -->
<!-- ============================================================ -->
<group name="local,windows,privilege_use,">

  <!-- EventID 4672: Special privileges assigned to new logon -->
  <rule id="100210" level="7">
    <if_sid>18104</if_sid>
    <id>4672</id>
    <description>Windows: Special privileges assigned to new logon (privileged account login)</description>
    <group>windows,privilege_escalation,authentication_success,pci_dss_10.2.5,</group>
  </rule>

  <!-- EventID 4688: New process created -->
  <rule id="100211" level="4">
    <if_sid>18104</if_sid>
    <id>4688</id>
    <description>Windows: New process created</description>
    <group>windows,process_monitor,</group>
  </rule>

  <!-- EventID 4673: Sensitive privilege use failure = suspicious -->
  <rule id="100212" level="8">
    <if_sid>18108</if_sid>
    <id>4673</id>
    <description>Windows: Sensitive privilege use - failed attempt (potential escalation)</description>
    <group>windows,privilege_escalation,pci_dss_10.2.5,</group>
  </rule>

</group>
